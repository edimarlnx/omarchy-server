=== SELinux acceptance — VM 'srvsel' — 2026-08-29T09:03:29-03:00 ===
mode under test: enforcing1

[PASS] the kernel initialised SELinux
      $ ls -d /sys/fs/selinux 2>&1; grep -o "lsm=[^ ]*" /proc/cmdline
      /sys/fs/selinux
      lsm=landlock,lockdown,yama,integrity,selinux,bpf

[PASS] lsm= names selinux, with lockdown ahead of it and bpf last
      $ grep -o "lsm=[^ ]*" /proc/cmdline
      lsm=landlock,lockdown,yama,integrity,selinux,bpf

[PASS] the cmdline came from inside the UKI, not from limine.conf
      $ ~/.lab-sudo grep -c "lsm=" /boot/limine.conf 2>/dev/null; echo "in-cmdline=$(grep -c "lsm=landlock" /proc/cmdline)"
      3
      in-cmdline=1

[PASS] the refpolicy-arch policy is loaded
      $ sestatus
      SELinux status:                 enabled
      SELinuxfs mount:                /sys/fs/selinux
      SELinux root directory:         /etc/selinux
      Loaded policy name:             refpolicy-arch
      Current mode:                   permissive
      Mode from config file:          permissive
      Policy MLS status:              disabled
      Policy deny_unknown status:     denied
      Memory protection checking:     actual (secure)
      Max kernel policy version:      35

[PASS] /etc/selinux/config says what the addon wrote
      $ grep -E "^(SELINUX|SELINUXTYPE)=" /etc/selinux/config; readlink -f /etc/selinux/config
      SELINUX=permissive
      SELINUXTYPE=refpolicy-arch
      /etc/selinux/config.refpolicy-arch

[PASS] the profile's own file contexts are in the policy store
      $ ~/.lab-sudo cat /etc/selinux/refpolicy-arch/contexts/files/file_contexts.local
      # Written by install/server/selinux-server.sh from
      # install/server/mac/selinux/local-fcontexts. Do not edit by hand:
      # the addon rewrites this file whole on every run.
      /usr/share/omarchy/bin(/.*)?	system_u:object_r:bin_t
      /var/lib/lastlog(/.*)?	system_u:object_r:lastlog_t

[PASS] and they are what the profile's commands actually carry
      $ ls -Z /usr/share/omarchy/bin/omarchy-server-update
      system_u:object_r:bin_t /usr/share/omarchy/bin/omarchy-server-update

[PASS] the base selinux addon did NOT pull setools or selinux-python in
      $ pacman -Qq setools selinux-python python-networkx python-scipy python-pandas 2>&1 | sort -u
      error: package 'python-networkx' was not found
      error: package 'python-pandas' was not found
      error: package 'python-scipy' was not found
      error: package 'selinux-python' was not found
      error: package 'setools' was not found

[PASS] and the policy tools are offered as their own addon
      $ omarchy-server-addon --list | grep -E "^selinux"
      selinux-tools
      selinux

[PASS] every binary that needs libselinux reaches it
      /usr/lib/systemd/systemd                 dlopen
      /usr/bin/ls                              linked
      /usr/bin/id                              linked
      /usr/bin/sudo                            linked
      /usr/lib/ssh/sshd-session                linked
      /usr/bin/useradd                         linked
      /usr/bin/mount                           linked
      /usr/lib/security/pam_selinux.so         linked

[PASS] the stock packages were replaced, not installed alongside
      $ pacman -Qq | grep -E "^(systemd|coreutils|util-linux|shadow|sudo|openssh|pam|pambase)(-selinux)?$" | sort
      coreutils-selinux
      openssh-selinux
      pam-selinux
      pambase-selinux
      shadow-selinux
      sudo-selinux
      systemd-selinux
      util-linux-selinux

[PASS] openssh was not downgraded by the rebuild
      $ pacman -Q openssh-selinux; sshd -V 2>&1 | head -1
      openssh-selinux 10.5p1-1
      OpenSSH_10.5p1, OpenSSL 3.6.4 25 Aug 2026

[PASS] init runs in a domain of its own
      $ tr -d "\0" </proc/1/attr/current
      system_u:system_r:init_t

[PASS] sshd runs in a domain of its own
      $ for p in $(pgrep -x sshd); do tr -d "\0" </proc/$p/attr/current; echo; done | sort -u
      system_u:system_r:sshd_t

[PASS] the ssh session is not simply sshd's own context
      $ id -Z; echo "---"; tr -d "\0" </proc/$$/attr/current
      user_u:user_r:user_t
      ---
      user_u:user_r:user_t

[PASS] the profile's pam_faillock settings survived the pambase replacement
      $ grep -n "pam_faillock" /etc/pam.d/system-auth; ls /etc/pam.d/*.pacnew 2>/dev/null
      3:auth       required                    pam_faillock.so preauth silent deny=10 unlock_time=120
      8:auth       [default=die]               pam_faillock.so authfail deny=10 unlock_time=120
      11:auth       required                    pam_faillock.so      authsucc
      12:# If you drop the above call to pam_faillock.so the lock will be done also

[PASS] the transition is pam_selinux's, and it is in the sshd stack
      $ grep -rn selinux /etc/pam.d/sshd /etc/pam.d/system-login 2>/dev/null
      /etc/pam.d/system-login:13:# pam_selinux.so close should be the first session rule
      /etc/pam.d/system-login:14:session    required   pam_selinux.so close
      /etc/pam.d/system-login:19:# pam_selinux.so open should only be followed by sessions to be executed in the user context
      /etc/pam.d/system-login:20:session    required   pam_selinux.so open

[PASS] /etc and /usr carry real labels, not unlabeled_t
      $ ls -Zd /etc /usr /usr/bin/sshd /etc/shadow
            system_u:object_r:etc_t /etc
         system_u:object_r:shadow_t /etc/shadow
            system_u:object_r:usr_t /usr
      system_u:object_r:sshd_exec_t /usr/bin/sshd

[PASS] findutils here cannot do the sweep, which is why the check below does not use it
      $ find /etc -maxdepth 0 -context "*" 2>&1 | head -1; echo "exit=$?"
      find: invalid predicate -context: SELinux is not enabled.
      exit=0

[FAIL] no file under /etc /usr /var has a label the policy disagrees with
      $ ~/.lab-sudo restorecon -nvR /etc /usr /var 2>&1 | head -20; echo "wrong=$(~/.lab-sudo restorecon -nvR /etc /usr /var 2>/dev/null | wc -l)"
      Would relabel /etc/docker from user_u:object_r:etc_t to user_u:object_r:container_config_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.btrfs/snapshots from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.native/snapshots from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/1 from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/1/fs from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/1/fs/hello from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/1/work from system_u:object_r:container_var_lib_t to system_u:object_r:container_ro_file_t
      Would relabel /var/lib/docker/volumes from system_u:object_r:container_file_t to system_u:object_r:container_var_lib_t
      Would relabel /var/lib/docker/volumes/backingFsBlockDev from system_u:object_r:container_file_t to system_u:object_r:container_var_lib_t
      Would relabel /var/lib/docker/volumes/metadata.db from system_u:object_r:container_file_t to system_u:object_r:container_var_lib_t
      wrong=11

[PASS] /home/<user> is labelled user_home_dir_t, not unlabeled
      $ ls -Zd /home /home/*
        system_u:object_r:home_root_t /home
      user_u:object_r:user_home_dir_t /home/omarchy

[PASS] and nothing under /home has a label the policy disagrees with
      $ ~/.lab-sudo restorecon -nvR /home /root 2>&1 | head -10; echo "wrong=$(~/.lab-sudo restorecon -nvR /home /root 2>/dev/null | wc -l)"
      wrong=0

[PASS] the relabel flag was cleared, so the next boot does not repeat it
      $ ls -l /.autorelabel 2>&1
      ls: cannot access '/.autorelabel': No such file or directory

[PASS] the first-boot relabel unit ran and succeeded
      $ systemctl is-enabled omarchy-server-selinux-relabel.service; ~/.lab-sudo journalctl -u omarchy-server-selinux-relabel.service --no-pager -o cat | tail -5
      enabled
      omarchy-server-selinux-relabel.service: Deactivated successfully.
      Finished Relabel the filesystem for SELinux (one-shot).
      omarchy-server-selinux-relabel.service: Consumed 784ms CPU time over 1.129s wall clock time, 87.3M memory peak.
      Relabel the filesystem for SELinux (one-shot) skipped, unmet condition check ConditionPathExists=/.autorelabel
      Relabel the filesystem for SELinux (one-shot) skipped, unmet condition check ConditionPathExists=/.autorelabel

--- switching to enforcing ---
[PASS] enforcing is refused from a session that could not undo it
      $ ~/.lab-sudo omarchy-server-selinux enforcing 2>&1 | head -14; echo "mode=$(getenforce)"
      omarchy-server-selinux: refusing to enforce.
        - this session runs as user_u:user_r:user_t, and that role cannot
        -     setenforce. Going enforcing from here is a ONE-WAY door:
        -     `sudo omarchy-server-selinux permissive` will be refused by the
        -     policy afterwards, because sudo does not change the SELinux role
        -     on this profile (seusers maps __default__:user_u).
        -     The ways back all need console access. Use --force only if you
        -     have one.
        - 10 denials on the login/sudo path or against an
        -     unlabeled target. These are the ones that lock you out:
        -     audit: type=1400 audit(1788005009.531:3): avc:  denied  { search } for  pid=583 comm="sudo" name="559" dev="proc" ino=7958 scontext=user_u:user_r:user
        -     audit: type=1400 audit(1788005009.531:4): avc:  denied  { read } for  pid=583 comm="sudo" name="stat" dev="proc" ino=8586 scontext=user_u:user_r:user_
        -     audit: type=1400 audit(1788005009.531:5): avc:  denied  { open } for  pid=583 comm="sudo" path="/proc/559/stat" dev="proc" ino=8586 scontext=user_u:us
      
      mode=Permissive

SELinux is now enforcing, and enforcing on the next boot.

If this locks something out, the ways back, in order:
  NOT from an ssh session. This machine maps its operator to
  user_u, sudo does not change that, and user_r is not granted
  security { setenforce } -- so `omarchy-server-selinux permissive`
  will be refused by the policy from here on. Measured, not assumed.
  * boot the previous snapper snapshot from the Limine menu and
    undo it there. The profile keeps five.
  * boot the installer medium, mount the root subvolume and set
    SELINUX=permissive in its /etc/selinux/config.

NOT the kernel command line. `enforcing=0` cannot be added at the
Limine menu on this profile: the cmdline lives inside the UKI, and a
UKI entry ignores the `cmdline:` in limine.conf.

[PASS] the machine is enforcing
      $ getenforce
      Enforcing

[FAIL] and it will still be enforcing after a reboot
      $ grep "^SELINUX=" /etc/selinux/config
      SELINUX=permissive

[PASS] and this session cannot switch it back, as the refusal warned
      $ ~/.lab-sudo omarchy-server-selinux permissive 2>&1 | tail -3; echo "mode=$(getenforce)"
      setenforce:  security_setenforce() failed:  Permission denied
      omarchy-server-selinux: setenforce failed.
      mode=Enforcing

[PASS] sudo still works
      $ ~/.lab-sudo id -un
      root

[PASS] a snapper snapshot can be taken
      $ ~/.lab-sudo snapper -c root create -d selinux-acceptance --print-number && ~/.lab-sudo snapper -c root list | tail -3
      3
      1 | single |       | Sat Aug 29 09:02:57 2026 | root |         | selinux-acceptance |
      2 | single |       | Sat Aug 29 09:03:11 2026 | root | number  | 4.0.1-4            |
      3 | single |       | Sat Aug 29 09:03:32 2026 | root |         | selinux-acceptance |

[PASS] pacman can run a transaction
      $ ~/.lab-sudo pacman -Sy --noconfirm >/dev/null 2>&1; ~/.lab-sudo pacman -Q pacman-contrib && echo pacman-ok
      pacman-contrib 1.13.1-1
      pacman-ok

[FAIL] the docker addon installs and runs a container
      $ ~/.lab-sudo omarchy-server-addon docker >/tmp/addon-docker.log 2>&1; echo "addon-exit=$?"; tail -3 /tmp/addon-docker.log; ~/.lab-sudo docker run --rm hello-world >/tmp/hello.log 2>&1; echo "run-exit=$?"; grep -c "Hello from Docker" /tmp/hello.log
      addon-exit=1
      :: Synchronizing package databases...
      error: failed to create temporary download directory /var/lib/pacman/sync/download-PoLv70: Permission denied
      error: failed to synchronize all databases (failed to initialize download)
      run-exit=1
      0

[PASS] the fwall addon installs
      $ ~/.lab-sudo omarchy-server-addon fwall 2>&1 | tail -3; command -v fwall
      :: Synchronizing package databases...
      error: failed to create temporary download directory /var/lib/pacman/sync/download-jW0X6u: Permission denied
      error: failed to synchronize all databases (failed to initialize download)
      /usr/bin/fwall

[FAIL] the firewall still answers
      $ ~/.lab-sudo ufw status verbose | head -8
      ERROR: Couldn't determine iptables version

[FAIL] a login on the serial console reaches a shell in a user domain
      $ ~/.lab-sudo systemctl is-active serial-getty@ttyS0.service
      Failed to connect to system scope bus via local transport: Operation not permitted (consider using --machine=<user>@.host --user to connect to bus of other user)

[FAIL] omarchy-server-update completes
      $ ~/.lab-sudo omarchy-server-update >/tmp/update.log 2>&1; echo "update-exit=$?"; tail -8 /tmp/update.log
      update-exit=1
      [0m[?25h]3008;end=c0b0083db58f40fbafddffbe058defd0\[1;31merror: [0mfailed to create temporary download directory /var/lib/pacman/sync/download-sdf96T: Permission denied
      [1;31merror: [0mfailed to synchronize all databases (failed to initialize download)
      
      [0;31mSomething went wrong during the update!
      
      Please review the output above carefully, correct the error, and retry the update.
      
      If you need assistance, get help from the community at https://omarchy.org/discord[0m

--- AVC denials accumulated by the workload above ---
== AVC denials since boot ==
      audit: type=1400 audit(1788005009.531:3): avc:  denied  { search } for  pid=583 comm="sudo" name="559" dev="proc" ino=7958 scontext=user_u:user_r:user_sudo_t tcontext=system_u:system_r:sshd_t tclass=dir permissive=1
      audit: type=1400 audit(1788005009.531:4): avc:  denied  { read } for  pid=583 comm="sudo" name="stat" dev="proc" ino=8586 scontext=user_u:user_r:user_sudo_t tcontext=system_u:system_r:sshd_t tclass=file permissive=1
      audit: type=1400 audit(1788005009.531:5): avc:  denied  { open } for  pid=583 comm="sudo" path="/proc/559/stat" dev="proc" ino=8586 scontext=user_u:user_r:user_sudo_t tcontext=system_u:system_r:sshd_t tclass=file permissive=1
      audit: type=1400 audit(1788005010.106:6): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="tmpfs" ino=1 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:tmpfs_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.106:7): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="cgroup2" ino=1 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:cgroup_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.106:8): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="pstore" ino=4148 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:pstore_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.106:9): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="hugetlbfs" ino=717 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:hugetlbfs_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.106:10): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="tracefs" ino=1 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:tracefs_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.106:11): avc:  denied  { getattr } for  pid=689 comm="restorecon" name="/" dev="debugfs" ino=1 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:debugfs_t tclass=filesystem permissive=1
      audit: type=1400 audit(1788005010.107:12): avc:  denied  { getattr } for  pid=689 comm="restorecon" path="/etc/pacman.d/gnupg/S.dirmngr" dev="vda2" ino=74280 scontext=user_u:user_r:user_sudo_t tcontext=system_u:object_r:etc_t tclass=sock_file permissive=1
      
      == summary: count, source domain, target context, class, permissions ==
           1  user_u:user_r:user_sudo_t -> system_u:system_r:sshd_t : file { read }
           1  user_u:user_r:user_sudo_t -> system_u:system_r:sshd_t : file { open }
           1  user_u:user_r:user_sudo_t -> system_u:system_r:sshd_t : dir { search }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:tracefs_t : filesystem { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:tmpfs_t : filesystem { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:pstore_t : filesystem { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:hugetlbfs_t : filesystem { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:etc_t : sock_file { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:debugfs_t : filesystem { getattr }
           1  user_u:user_r:user_sudo_t -> system_u:object_r:cgroup_t : filesystem { getattr }
      
      To turn these into a policy module (audit2allow is in the
      `selinux-tools` addon, and does not have to be on this machine):
          journalctl -k -b -g 'avc: *denied' -o cat | audit2allow -m omarchy_server_local
      Read what it produces before installing it. A rule that allows a denial
      the machine should not have made is a hole, not a fix.

[FAIL] enforcing: something was denied under the workload
      10 AVC records this boot — see the dump above

--- rebooting ---
boot id before: cff2fd09-229a-428d-a63b-33b5aac7cb07
boot id after:  18a52742-eb64-4940-95d4-fbbc2d48389c

[PASS] the machine came back over ssh after a reboot
      boot_id cff2fd09-229a-428d-a63b-33b5aac7cb07 -> 18a52742-eb64-4940-95d4-fbbc2d48389c

[PASS] SELinux is still in the mode it was left in
      $ getenforce
      Permissive

[PASS] and the boot produced no new denials before login
      $ journalctl -k --no-pager -b -g 'avc: *denied' -o cat | wc -l
      0

=== 33 passed, 7 failed ===
